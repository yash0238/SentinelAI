# Streamlit theme for the SentinelAI dashboard — dark "command centre" look.
[theme]
base = "dark"
primaryColor = "#4da6ff"
backgroundColor = "#0b0f1a"
secondaryBackgroundColor = "#141a2b"
textColor = "#e6ebf5"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
