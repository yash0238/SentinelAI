/**
 * SentinelAI Dashboard — Reports.
 *
 * Sortable, filterable table of logged fraud reports (GET /reports) with
 * risk level, scam type, language, and status. Row click -> detail drawer.
 *
 * TODO
 *   [ ] Render <ReportsTable /> with filters (type, risk, date).
 *   [ ] Wire pagination to the backend.
 */
