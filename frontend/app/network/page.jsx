/**
 * SentinelAI Dashboard — Fraud Network Graph explorer.
 *
 * Visualises money-mule / fraud-ring clusters from GET /graph/clusters and
 * lets an officer expand an entity's ego network (GET /graph/entity/{id}).
 * A "Generate Intelligence Package" action calls GET /graph/package/{id}.
 *
 * TODO
 *   [ ] Render <NetworkGraph /> with cluster data.
 *   [ ] Side panel showing selected node metadata + provenance.
 */
