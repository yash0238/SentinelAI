/**
 * SentinelAI Dashboard — API client helpers.
 *
 * Thin fetch wrappers around the FastAPI backend. Base URL comes from
 * NEXT_PUBLIC_API_BASE_URL. Used with SWR in the pages/components.
 *
 * Planned helpers:
 *   getStats(), getHotspots(), listReports(filters),
 *   getClusters(), getEntity(id), getIntelPackage(id).
 *
 * TODO
 *   [ ] Implement a small fetch wrapper with error handling.
 *   [ ] Export the helpers above.
 */
